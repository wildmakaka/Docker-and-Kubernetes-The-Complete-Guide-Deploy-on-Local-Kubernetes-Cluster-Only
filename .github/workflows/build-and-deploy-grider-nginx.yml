name: Build and Deploy API
on: 
  pull_request:
    branches:
    - master
  push:
    branches:
    - master

jobs:
  build:
    name: Build and Deploy Grider Nginx
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@master
    - name: Build the Docker container image
      run: docker build ./apps/nginx -f ./apps/nginx/Dockerfile -t techhead/grider-nginx:latest
      
    - name: Push the image to hub.docker.com
      run: |
        docker login -u techhead -p "${DOCKER_HUB_PASS}"
        docker push techhead/grider-nginx:latest
      env:
        DOCKER_HUB_PASS: ${{ secrets.DOCKER_HUB_PASS }}
